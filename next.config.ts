/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com',  'maps.googleapis.com'], // Allow Unsplash's image CDN
  },
};

export default nextConfig;
