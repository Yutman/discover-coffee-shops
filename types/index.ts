export type CoffeeStoreType = {
    id: string;
    name: string;
    imgUrl: string;
    address: string;
};

export type GooglePlacesType = {
    photos: boolean;
    id: string;
    name: string;
    address: string;
    imgUrl?: string;
};

