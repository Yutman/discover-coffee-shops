export type CoffeeStoreType = {
    id: string;
    name: string;
    imgUrl: string;
    address: string;
};

export type GooglePlacesType = {
    id: string;
    name: string;
    address: string;
    imgUrl?: string;
};
